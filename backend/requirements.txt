# Direct dependencies. Exact resolved versions are in requirements.lock.txt.
fastapi
uvicorn[standard]

sqlalchemy>=2.0
psycopg[binary]
alembic

# [email] pulls email-validator, which EmailStr needs.
pydantic[email]
pydantic-settings

python-jose[cryptography]
# bcrypt directly, not passlib: passlib is unmaintained and breaks on bcrypt 5.
bcrypt
python-multipart

pytest
httpx
